namespace Shared.Libraries.Extensions
{
    public static class FileExtension
    {
        // public static string read_file(this object source, string file_path)
        // {
        //     if (File.Exists(file_path)) return File.ReadAllText(file_path);
        //
        //     return string.Empty;
        // }
    }
}