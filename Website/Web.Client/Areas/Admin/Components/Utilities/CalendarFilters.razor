@using Web.Shared.Models
@inherits CalendarFiltersRazor


<div id="calendar_filters" style="display:none;">
    form_open();
    form_hidden('calendar_filters',true);
    <div class="row">
        <div class="col-md-3">
            <div class="checkbox">
                <input type="checkbox" value="1" name="events" id="cf_events" if($this->input->post('events')){ ' checked';} >
                <label for="cf_events"> helper.label('events'); </label>
            </div>
            @if (context.Options.option_value<bool>("show_tasks_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="tasks" id="cf_tasks" if($this->input->post('tasks')){ ' checked';} >
                    <label for="cf_tasks"> helper.label('tasks'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("show_projects_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="projects" id="cf_projects" if($this->input->post('projects')){ ' checked';} >
                    <label for="cf_projects"> helper.label('projects'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("show_invoices_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="invoices" id="cf_invoices" if($this->input->post('invoices')){ ' checked';} >
                    <label for="cf_invoices"> helper.label('invoices'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("show_estimates_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="estimates" id="cf_estimates" if($this->input->post('estimates')){ ' checked';} >
                    <label for="cf_estimates"> helper.label('estimates'); </label>
                </div>
            }
        </div>
        <div class="col-md-3">
            @if (context.Options.option_value<bool>("show_proposals_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="proposals" id="cf_proposals" if($this->input->post('proposals')){ ' checked';} >
                    <label for="cf_proposals"> helper.label('proposals'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("show_contracts_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="contracts" id="cf_contracts" if($this->input->post('contracts')){ ' checked';} >
                    <label for="cf_contracts"> helper.label('contracts'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("show_customer_reminders_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="customer_reminders" id="cf_customers_reminders" if($this->input->post('customer_reminders')){ ' checked';} >
                    <label for="cf_customers_reminders"> helper.label('show_customer_reminders_on_calendar'); </label>
                </div>
            }

            @if (context.Options.option_value<bool>("show_expense_reminders_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="expense_reminders" id="cf_expenses_reminders" if($this->input->post('expense_reminders')){ ' checked';} >
                    <label for="cf_expenses_reminders"> helper.label('calendar_expense_reminder'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("showhelper.labelead_reminders_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="lead_reminders" id="cfhelper.labeleads_reminders" if($this->input->post('lead_reminders')){ ' checked';} >
                    <label for="cfhelper.labeleads_reminders"> helper.label('showhelper.labelead_reminders_on_calendar'); </label>
                </div>
            }
        </div>
        <div class="col-md-3">

            @if (context.Options.option_value<bool>("show_estimate_reminders_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="estimate_reminders" id="cf_estimates_reminders" if($this->input->post('estimate_reminders')){ ' checked';} >
                    <label for="cf_estimates_reminders"> helper.label('show_estimate_reminders_on_calendar'); </label>
                </div>
            }

            @if (context.Options.option_value<bool>("show_invoice_reminders_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="invoice_reminders" id="cf_invoices_reminders" if($this->input->post('invoice_reminders')){ ' checked';} >
                    <label for="cf_invoices_reminders"> helper.label('show_invoice_reminders_on_calendar'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("show_credit_note_reminders_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="credit_note_reminders" id="cf_credit_note_reminders" if($this->input->post('credit_note_reminders')){ ' checked';} >
                    <label for="cf_credit_note_reminders"> helper.label('show_credit_note_reminders_on_calendar'); </label>
                </div>
            }
            @if (context.Options.option_value<bool>("show_proposal_reminders_on_calendar"))
            {
                <div class="checkbox">
                    <input type="checkbox" value="1" name="proposal_reminders" id="cf_proposal_reminders" if($this->input->post('proposal_reminders')){ ' checked';} >
                    <label for="cf_proposal_reminders"> helper.label('show_proposal_reminders_on_calendar'); </label>
                </div>
            }
        </div>
        <div class="col-md-3 text-right">
            <a class="btn btn-default" href="  site_url($this->uri->uri_string()); "> helper.label('clear'); </a>
            <button class="btn btn-success" type="submit"> helper.label('apply'); </button>
        </div>

    </div>
    <hr class="mbot15"/>
    <div class="clearfix"></div>
    form_close();
</div>