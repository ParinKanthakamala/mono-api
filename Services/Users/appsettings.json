{
  "config": {
    "RabbitMQ": {
      "name": "users",
      "host": "localhost",
      "port": 5672,
      "account": "guest",
      "password": "guest"
    },
    "database": {
      "connection": "server=localhost;user=root;password=password;database=perfex;convert zero datetime=True"
    }
  }
}